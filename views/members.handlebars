<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <h2>Welcome <span class="member-name"> {{ this.name }} </span></h2>


            <div class="inQuote">{{ this.text }}</div>
            <div class="author">{{ this.author }}</div>
            <br>

        </div>
    </div>
</div>
<hr>
<br>
<!-- place cards here -->

<div class="container">
    <div class="row">
        <div class="col-sm-5 ">
            <h3 id="budReq">Buddy Requests</h3>
            <br />

            <!-- CARDS -->

            {{> cards }}
            {{#each cards}}
            {{ this.name }}
            {{ this.zodiac }}
            {{ this.notes }}
            {{ this.buddies }}
            {{ this.subject }}
            {{ this.preference }}
            {{/each}}
            {{!-- <div class="card">

                <div class="card-image"> {{ this.avatar }} </div>
                <div class="card-text">

                    <h2> {{ this.name }} Johanna Cash</h2>
                    <p> {{ this.zodiac }} Capricorn</p>
                    <p>{{ this.notes }}I need to study up on handlebars, sequelize and api routes</p>
                    <div><button id="reqSubmit" type="button" class="btn btn-primary">Let's Study!</button></div>
                </div>
                <div class="card-stats">
                    <div class="stat">
                        <div class="value">Buddies</div>
                        <div class="type">{{ this.buddies }} 3</div>
                    </div>
                    <div class="stat border">
                        <div class="value">Category</div>
                        <div class="type">{{ this.subject }} Coding</div>
                    </div>
                    <div class="stat">
                        <div class="value">Preference</div>
                        <div class="type">{{ this.preference }}Zoom</div>
                    </div>

                </div>
            </div> --}}
        </div>




    </div>
</div>
</div>


<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script type="text/javascript" src="js/members.js"></script>
<!-- Quote SCRIPT SHOULD BE HERE -->
<script src="js/script.js"></script>